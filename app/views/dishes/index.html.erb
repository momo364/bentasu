<p>おかず一覧ページ</p>
<% @dishes.each do |dish| %>
  <tr>
    <td><%= dish.name %></td>
    <td><%= dish.price %></td>
    <% dish.allergies.order("id").each do |allergy| %>
      <td><%= allergy.name %></td>、
    <% end %>
  </tr>
  </br>
<% end %>
